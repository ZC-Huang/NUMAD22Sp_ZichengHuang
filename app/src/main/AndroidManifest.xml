<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.neu.numad22sp_zichenghuang">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_zh"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_zh_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NUMAD22Sp_ZichengHuang">
        <activity
            android:name=".LinkCollector"
            android:exported="false" />
        <activity
            android:name=".SixButtons"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>